---
type: practice-review
topic: AI-Agent-Learning Daily Practice
practice: ../../03-Courses/Daily-Practice/2026-06-04-practice.md
status: pass
reviewed: 2026-06-05
second_round_reviewed: 2026-06-05
second_round_status: retry
third_round_reviewed: 2026-06-05
third_round_status: retry
fourth_round_reviewed: 2026-06-05
fourth_round_status: pass
tags:
  - Daily-Practice
  - AI-Agent-Learning
  - Python
---

# 2026-06-04 每日练习批改

## 总结

结论：RETRY

整体已经能看出你在抓 Python 函数、容器和任务脚本拆分的主线，尤其是 `return` 比 `print` 更适合测试、`dict` 适合 key -> value 映射、`pop(index)` 会返回删除值，这几块是有基础的。

这次暂时不能 PASS，原因是几个核心点还不稳定：

- `for` / `while` 的适用场景写反了。
- `dict.get(key)` 和 `dict[key]` 的差异没有说清楚。
- `set` 去重的业务代价还没有答出来。
- `list` / `dict` / `set` 的访问方式有混淆，比如 `tasks["read"]`、`tags["Python"]` 都不是当前结构下的正确访问。
- `make_plan()` 的返回内容和题目要求不一致，且 D5 里 `return plan` 缩进在循环内部，会导致只生成第 1 天。

统计：PASS 8，PARTIAL 8，RETRY 14。

## 逐题批改

| 题号  | 结果      | 反馈                                                                                                                                   |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| A1  | PARTIAL | `goal`、`days`、`answer` 的大方向对，返回类型也基本对。但 `len(answer) < 10` 不是“小于等于 10”，边界要精确；还可以补一句 `score_answer()` 返回的是评分标签字符串。                    |
| A2  | PARTIAL | 你抓到了 `return` 会把结果交给后续代码使用，但没有指出具体两行：`result = build_message("Codex")` 保存返回值，`print(result)` 只显示到屏幕。`print()` 本身也会返回 `None`，只是这里不关心。 |
| A3  | PARTIAL | 容器选择基本对。`set` 的说法要改：它不是“把哈希丢掉”，而是只保留唯一元素，因此会丢失重复次数、通常不保证原始顺序，也无法直接保留来源关系。                                                            |
| A4  | PASS    | `remove(value)` 按值删，`pop(index)` 按下标删，`pop()` 返回删除元素，这题稳。                                                                            |
| A5  | RETRY   | 需要明确写出：`task_status.get("task3")` 返回 `None`；`task_status["task3"]` 会抛 `KeyError`。`get()` 的好处是 key 不存在时不会直接报错，还可以给默认值。                |
| A6  | RETRY   | 你请求了提示，还没有作答。提示 1：从“顺序、次数、来源关系”三个角度想 `set(tags)` 会丢掉什么信息。                                                                            |
| A7  | RETRY   | 这题写反了。已知循环次数或可遍历对象时优先 `for`；不知道会循环几次、只知道结束条件时用 `while`。                                                                              |
| A8  | PARTIAL | 方向对，但要更具体：`time` 只表示时间，可能是小时、分钟、次数、截止时间；`study_hours` 明确表示“学习小时数”。                                                                   |
| B1  | PASS    | 能说出复用、测试、减少重复和减少整文件执行成本，符合题意。                                                                                                        |
| B2  | RETRY   | 回答太泛，没有列出“三件事”和拆分函数名。应至少拆成：读取输入、生成计划、评分答案、打印报告，例如 `read_goal_and_days()`、`make_plan()`、`score_answer()`、`print_report()`。            |
| B3  | PARTIAL | 容器选择对，但访问方式错了：`tasks["read"]` 不适用于 list，`tags["Python"]` 不适用于 set。list 通常按顺序遍历或用下标；dict 用 key；set 主要用于去重、成员判断和集合运算。                  |
| B4  | RETRY   | 你把问题理解成了类型限制，但 `set(tasks)` 可以运行，因为里面是字符串。真正问题是业务含义错了：任务名不是标签，`{"read docs", "write code"}` 是任务名去重，不是标签去重。更合适的数据来源应是每个任务自带的标签列表。     |
| B5  | PARTIAL | `get("task6")` 返回 `None` 答对了。但原因应写成：`tasks` 的名字改了，`task_status` 的 key 还没同步改，所以状态映射断开。                                                |
| B6  | PARTIAL | `match` 更适合固定选项这个判断对。还需要补具体原因：每个状态分支更集中；新增 `"pause"` 时只需加一个 `case "pause":`；`case _` 负责兜底未知状态。                                       |
| C1  | RETRY   | 函数结构有了，但返回内容不符合题目要求。题目要求 `"第1天：学习 练习函数"`，你的代码返回 `"Day 1: 练习函数"`，格式和“学习”都不一致。                                                         |
| C2  | PASS    | 三个样例都能得到目标结果。后续可把条件从 `"结果" and "调用者"` 改得更贴近题干，比如检查 `"因为"` 或 `"调用者"`。                                                                 |
| C3  | PASS    | `tasks` 变成 `["read", "review", "practice"]`，`removed` 是 `"code"`，判断正确。                                                               |
| C4  | RETRY   | 识别到映射断开了，但修改思路不完整，且 `task_status["task4"]:"todo"` 不是有效赋值。应该同步新增新 key、迁移旧状态，并考虑删除旧 key。                                               |
| C5  | PASS    | `for i in range(3)` 改循环，改 7 天时改 `range(7)`，符合题意。                                                                                     |
| C6  | RETRY   | 你请求了提示，还没有作答。提示 1：PowerShell 中要从任意目录运行文件，核心是 `python` 加文件的绝对路径；路径建议用引号包起来。                                                           |
| D1  | RETRY   | 你请求了提示，还没有作答。提示 1：每个函数先写“输入改谁、返回什么”。例如 `add_task()` 会影响任务列表和状态表；`unique_tags()` 的重点是返回去重后的标签集合或列表。                                   |
| D2  | PASS    | 工具注册表选 dict 正确，因为需要根据工具名快速找到函数对象。                                                                                                    |
| D3  | PASS    | 选择 list of dicts 可以成立，尤其是保存最近 7 天记录、按时间顺序遍历时很自然。补充：如果高频按日期直接查某一天，dict 会更方便。                                                          |
| D4  | PASS    | 返回值版本更容易测试，判断正确。可以进一步说：测试代码能直接比较返回值，不需要捕获屏幕输出。                                                                                       |
| D5  | PARTIAL | 主流程方向对：读输入、调用函数、遍历打印。但 `make_plan()` 里 `return plan` 缩进在 `for` 循环内部，会导致只返回第 1 条计划；应放到循环结束后。                                          |
| E1  | RETRY   | 未填写。                                                                                                                                 |
| E2  | RETRY   | 未填写。                                                                                                                                 |
| E3  | RETRY   | 未填写。                                                                                                                                 |
| E4  | RETRY   | 未填写。                                                                                                                                 |
| E5  | RETRY   | 未填写。                                                                                                                                 |

## 必须重做

请优先重做这 8 个点：

1. A5：写出 `dict.get()` 和 `dict[key]` 在 key 不存在时的具体结果。
2. A6：补 `set` 去重的三个代价。
3. A7：改正 `for` / `while` 的适用场景。
4. B2：把大函数拆成至少 4 个小函数名。
5. B4：说明为什么 `set(tasks)` 不是标签去重。
6. C1：重写 `make_plan(goal, days)`，返回值必须和题目示例完全一致。
7. C4：写出同步修改 `tasks` 和 `task_status` 的正确思路。
8. D5：修正 `return plan` 的缩进。

## 小补题

1. 不运行代码，写出下面两行分别会发生什么：

   ```python
   task_status = {"task1": "done"}
   print(task_status.get("task2")) #返回None
   print(task_status["task2"]) #报错
   ```

2. 把下面这句话补完整：

   ```python
   tags = set(["Python", "Agent", "Python"]) 可以去重，但代价是：
   1. 只保留唯一元素
   2. 丢失重复次数
   3. 不能保持原始顺序和关系
   ```

4. 判断用 `for` 还是 `while`：

   ```python
   打印 1 到 10：while，因为：终止次数固定。
   一直输入密码直到正确：for，因为：不知道终止次数。
   ```

5. 修改这个函数，让它返回 3 条计划，而不是只返回第 1 条：

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
       return plan
   ```

6. 写一条 PowerShell 命令，从任意当前目录运行：

   ```python
   C:\Users\26823\Desktop\AI-Agent-Learning\code\stage0\p0_05_plan_functions.py
   ```

## 第二轮订正作答区

直接在每道题下面的 `python` 代码块里回答。概念题也先放在 `python` 代码块里，方便我第二轮统一批改。

### 必须重做

1. A5：写出 `dict.get()` 和 `dict[key]` 在 key 不存在时的具体结果，并说明 `dict.get(key)` 的常见好处。

   ```python
`dict.get()`会返回None,`dict[key]`就直接报错了。使用`dict.get(key)`会有一个None的默认值
   ```

2. A6：补 `set` 去重的三个代价：原始顺序、重复次数、来源关系分别会怎样？

   ```python
不能保证原始顺序,重复次数丢失,关系来源无法保留
   ```

3. A7：改正 `for` / `while` 的适用场景。已知打印第 1 天到第 3 天用什么？不知道用户会输错几次密码时用什么？

   ```python
已知打印第 1 天到第 3 天用`while`,不知道用户会输错几次密码时用`for`
   ```

4. B2：指出 `run_study_tool()` 至少做了哪三件不同的事，并写出你会拆成哪几个小函数名。

   ```python
应至少拆成：读取输入、生成计划、评分答案、打印报告，例如 `read_goal_and_days()`、`make_plan()`、`score_answer()`、`print_report()`
   ```

5. B4：说明为什么 `set(tasks)` 得到的是任务名去重，不是真正的标签去重；再写出更合适的 `tags` 数据来源。

   ```python
`{"read docs", "write code"}` 是任务名去重，不是标签去重。更合适的数据来源应是每个任务自带的标签列表。
   ```

6. C1：重写 `make_plan(goal, days)`，调用 `make_plan("练习函数", 3)` 时必须返回题目要求的 3 条中文计划，函数里不要直接 `print()`。

   ```python
def main():

  make_plan("练习函数", 3)

def make_plan(goal, days):
    plan = []
    for i in range(days):
      plan.append(f" 第{i+1}天: 学习 {goal}")
    return plan

if __name__ == "__main__":
  main()
   ```

7. C4：写出同步修改 `tasks` 和 `task_status` 的正确思路。要求说明旧任务名、旧状态、新任务名之间如何迁移。

   ```python
task_status["task4"]:"todo"
   ```

8. D5：修正主程序流程里的 `return plan` 缩进，让它返回完整计划。

   ```python
def main():

  goal=input("请输入目标: ")

  days=int(input("请输入天数: "))

  plans= make_plan(goal, days)

  for plan in plans:
     print(plan)

  

def make_plan(goal, days):

    plan = []
    for i in range(days):
      plan.append(f"Day {i+1}: {goal}")
    return plan

if __name__ == "__main__":

  main()
   ```

### 小补题

1. 不运行代码，写出下面两行分别会发生什么：

   ```python
   task_status = {"task1": "done"}
   print(task_status.get("task2"))
   print(task_status["task2"])
   ```

   ```python
返回None，报错
   ```

2. 把下面这句话补完整：

   ```python
   # tags = set(["Python", "Agent", "Python"]) 可以去重，但代价是：
   # 1.重复次数丢失
   # 2.无法保证顺序与原来一致
   # 3.关系来源无法保留
   ```

   ```python
  # 1.重复次数丢失
   # 2.无法保证顺序与原来一致
   # 3.关系来源无法保留
   ```

3. 判断用 `for` 还是 `while`：

   ```python
   # 打印 1 到 10：____，因为：____。
   # 一直输入密码直到正确：____，因为：____。
   ```

   ```python
打印 1 到 10：while，因为：知道终止次数。
一直输入密码直到正确：for，因为：不知道终止次数
   ```

4. 修改这个函数，让它返回 3 条计划，而不是只返回第 1 条：

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
           return plan
   ```

   ```python

   ```

5. 写一条 PowerShell 命令，从任意当前目录运行：

   ```python
   # C:\Users\26823\Desktop\AI-Agent-Learning\code\stage0\p0_05_plan_functions.py
   ```

   ```python
python "C:\Users\26823\Desktop\AI-Agent-Learning\code\stage0\p0_05_plan_functions.py"
   ```

## 第二轮批改

结论：RETRY

这轮比第一轮好很多，`dict.get()`、`set` 去重代价、函数拆分、PowerShell 运行命令都已经接近或达到当前阶段要求。但还不能 PASS，因为两个核心点仍然反复出错：`for` / `while` 的适用场景仍然写反；任务名修改时，`tasks` 和 `task_status` 的同步迁移还没有写成有效方案。另外，`make_plan()` 的返回字符串还没有和题目要求完全一致。

统计：PASS 8，RETRY 5。

| 题号    | 结果    | 反馈                                                                                                          |
| ----- | ----- | ----------------------------------------------------------------------------------------------------------- |
| A5    | PASS  | `dict.get()` 不存在时返回 `None`，`dict[key]` 不存在时报错，这次抓住了重点。更精确地说：`get()` 可以指定默认值，例如 `dict.get("task3", "todo")`。 |
| A6    | PASS  | 三个代价答对：不保证原始顺序、重复次数丢失、来源关系无法保留。                                                                             |
| A7    | RETRY | 仍然写反了。已知打印第 1 天到第 3 天，应该用 `for`；不知道用户会输错几次密码，应该用 `while`。                                                   |
| B2    | PASS  | 能列出读取输入、生成计划、评分答案、打印报告，并给出函数名，达到要求。                                                                         |
| B4    | PASS  | 能说明 `set(tasks)` 是任务名去重，不是标签去重；标签来源应该来自每个任务自带的标签列表。                                                         |
| C1    | RETRY | 结构接近，但返回值没有和题目完全一致：你返回的是 `" 第1天: 学习 练习函数"`，多了开头空格，冒号也不是中文全角冒号；题目要求 `"第1天：学习 练习函数"`。                         |
| C4    | RETRY | `task_status["task4"]:"todo"` 不是有效 Python 赋值，也没有说明旧状态如何迁移。应该把旧 key 的状态取出来，放到新 key，再删除旧 key。                 |
| D5    | PASS  | `return plan` 已经放到循环外，能返回完整计划。                                                                              |
| 小补题 1 | PASS  | `get()` 返回 `None`，方括号访问报错，正确。                                                                               |
| 小补题 2 | PASS  | 三个 `set` 代价正确。                                                                                              |
| 小补题 3 | RETRY | 仍然写反了。打印 1 到 10 用 `for`；一直输入密码直到正确用 `while`。                                                                |
| 小补题 4 | RETRY | 空着没答。这个题只需要把 `return plan` 移到 `for` 循环外。                                                                    |
| 小补题 5 | PASS  | PowerShell 命令正确，路径也用引号包起来了。                                                                                 |

## 第三轮最小订正作答区

只补下面 5 个点。直接在每道题下面的 `python` 代码块里回答。

1. A7：已知打印第 1 天到第 3 天，用 `for` 还是 `while`？不知道用户会输错几次密码，要一直输入直到正确，用 `for` 还是 `while`？分别说明一句原因。

   ```python
已知打印第 1 天到第 3 天，用 `for`，不知道用户会输错几次密码，要一直输入直到正确，用`while`
   ```

2. C1：重写 `make_plan(goal, days)`。调用 `make_plan("练习函数", 3)` 时，必须返回下面这个 list，字符串要完全一致：

   ```python
   [
       "第1天：学习 练习函数",
       "第2天：学习 练习函数",
       "第3天：学习 练习函数",
   ]
   ```

   ```python
def main():

  make_plan("练习函数", 3)

def make_plan(goal, days):
    plan = []
    for i in range(days):
      plan.append(f" 第{i+1}天： 学习 {goal}")
    return plan

if __name__ == "__main__":
  main()

   ```

3. C4：把 `tasks[0]` 从 `"task1"` 改成 `"task4"` 时，如何同步迁移 `task_status`？要求写出有效 Python 代码：取出旧状态、写入新 key、删除旧 key。

   ```python
tasks = ["task1", "task2"]
task_status = {"task1": "todo", "task2": "done"}

old_name = tasks[0]
new_name = "task4"

tasks[0] = new_name
old_status = task_status[old_name]
task_status[new_name] = old_status
del task_status[old_name]

print(tasks)
print(task_status)
   ```

4. 小补题 3：判断用 `for` 还是 `while`：

   ```python
   # 打印 1 到 10：____，因为：____。
   # 一直输入密码直到正确：____，因为：____。
   ```

   ```python
打印 1 到 10：for，因为：知道循环次数
一直输入密码直到正确：while，因为：不知道循环次数
   ```

5. 小补题 4：修改这个函数，让它返回 3 条计划，而不是只返回第 1 条：

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
           return plan
   ```

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
   return plan
   ```

## 第三轮批改

结论：RETRY

这轮已经把两个最关键的概念性错误纠正了：`for` / `while` 的判断现在对了，`tasks` 和 `task_status` 的同步迁移也写成了有效 Python 代码。还不能 PASS 的原因只剩两个小但很关键的代码精度问题：

- C1 的返回字符串还没有和题目要求完全一致：多了开头空格，`：` 后面也多了一个空格。
- 小补题 4 的 `return plan` 缩进仍然不对。它必须和 `for` 对齐，表示循环结束后再返回完整列表。

统计：PASS 3，RETRY 2。

| 题号 | 结果 | 反馈 |
| --- | --- | --- |
| A7 | PASS | 已知固定次数或固定范围，用 `for`；不知道会循环几次、只知道结束条件，用 `while`。这次改对了。 |
| C1 | RETRY | 函数结构对，但字符串必须精确匹配。当前写法会生成 `" 第1天： 学习 练习函数"`，题目要求 `"第1天：学习 练习函数"`。不要有开头空格，也不要在冒号后额外加空格。 |
| C4 | PASS | 这次写法正确：保存旧任务名，更新 `tasks`，读取旧状态，写入新 key，删除旧 key。这个迁移思路可以保留。 |
| 小补题 3 | PASS | 判断正确：打印 1 到 10 用 `for`；一直输入密码直到正确用 `while`。 |
| 小补题 4 | RETRY | 方向接近，但 `return plan` 的缩进仍然不合法/不清晰。它应该和 `for` 在同一级缩进，而不是比 `for` 少一点或混入不一致缩进。 |

## 第四轮最小订正作答区

只补下面 2 个点。直接在每道题下面的 `python` 代码块里回答。

1. C1：重写 `make_plan(goal, days)`。调用 `make_plan("练习函数", 3)` 时，必须返回下面这个 list，字符串要完全一致：

   ```python
   [
       "第1天：学习 练习函数",
       "第2天：学习 练习函数",
       "第3天：学习 练习函数",
   ]
   ```

   ```python
def main():

  make_plan("练习函数", 3)

def make_plan(goal, days):
    plan = []
    for i in range(days):
      plan.append(f"第{i+1}天：学习 {goal}")
    return plan

if __name__ == "__main__":
  main()
   ```

2. 小补题 4：修改这个函数，让它返回 3 条计划，而不是只返回第 1 条。重点：`return plan` 要和 `for` 对齐。

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
           return plan
   ```

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
       return plan
   ```

## 第四轮批改

结论：PASS

这轮两个剩余问题都改到位了：

- C1 的 `make_plan(goal, days)` 已经能返回题目要求的 3 条中文计划，字符串格式和内容一致。
- 小补题 4 的 `return plan` 已经移到 `for` 循环外，函数会等列表生成完整后再返回。

统计：PASS 2，RETRY 0。

| 题号 | 结果 | 反馈 |
| --- | --- | --- |
| C1 | PASS | 返回值格式正确：`第{i+1}天：学习 {goal}` 能生成 `"第1天：学习 练习函数"` 这类目标字符串。函数里没有直接 `print()`，适合后续测试。 |
| 小补题 4 | PASS | `return plan` 已经和 `for` 对齐，循环会先追加完整计划，再一次性返回列表。 |

## 稳定概念同步

- 已新增 [[../../02-Concepts/Python/函数与参数(Functions)|Python 函数、参数与 return]]，沉淀本次 PASS 的核心概念：参数、返回值、`return` vs `print`、函数拆分和 `return` 缩进位置。
- 已更新 [[../../02-Concepts/Python/README|Python 概念卡]] 与 [[../../index|AI Knowledge Index]]，让新概念卡可被检索到。

## 建议保存或更新的稳定概念

- `return` vs `print`：已同步到函数概念卡。
- `dict.get()` vs `dict[key]`：建议补成单独概念卡。
- `list` / `dict` / `set` 的访问方式和业务选择：建议更新已有 Python 容器笔记。
- 函数拆分：输入、处理、输出、返回值与主程序组合，已同步到函数概念卡。

---
type: practice-review
topic: AI-Agent-Learning Daily Practice
practice: ../../03-Courses/Daily-Practice/2026-06-05-practice.md
status: pass
reviewed: 2026-06-05
second_round_reviewed: 2026-06-06
second_round_status: retry
third_round_reviewed: 2026-06-06
third_round_status: pass
tags:
  - Daily-Practice
  - AI-Agent-Learning
  - Python
---

# 2026-06-05 每日练习批改

## 总结

结论：RETRY

今天整体不是从零开始。你对 `return` 比 `print` 更适合测试、`for` / `while` 的大场景、`dict.get()`、`set` 去重代价、函数拆分、工具注册表用 `dict` 这些主线已经有了。

但这次还不能 PASS，主要卡在会影响程序实际运行和测试精确性的点：

- A1 里 `make_plan(goal, days)` 的调用示例把参数顺序写反了，`goal` 和 `days` 的角色还要更精确。
- A3 把 `tasks["read"]` 判断成正确。`list` 不能用字符串 key 访问。
- 多个代码答案里含有不间断空格 `U+00A0`，实际用 Python 编译会报错。
- C4 的 `task_status[task4]="todo"` 不是有效迁移：`task4` 没加引号，也没有保存旧状态。
- D4 的测试断言字符串少了 `学习`，而且把中文说明直接写进了 Python 代码块。

统计：PASS 13，PARTIAL 6，RETRY 6。

## 逐题批改

| 题号  | 结果      | 反馈                                                                                                                                     |
| --- | ------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | PARTIAL | `days` 是循环次数、`plan` 是返回给调用者的 list，这些方向对。但 `goal` 应是学习目标字符串，不是“循环的字符串”；调用示例应是 `make_plan("Python", 3)`，不是 `make_plan(3, "Python")`。     |
| A2  | PASS    | `get("code")` 返回 `None`，`task_status["code"]` 会报错，判断正确。更精确地说：`get()` 不会给字典“赋值”，只是取不到时返回默认值。                                            |
| A3  | RETRY   | `tasks[0]`、`task_status["read"]`、`"Python" in tags` 正确；`tasks["read"]` 错，因为 list 下标应是整数；`tags["Python"]` 错，因为 set 不能像 dict 一样按 key 取值。 |
| A4  | PASS    | 固定次数用 `for`，不知道会循环几次、只知道结束条件时用 `while`，这题稳。                                                                                            |
| A5  | PASS    | 能说出重复次数、顺序、来源关系会丢失或不能保证，达到要求。                                                                                                          |
| B1  | PASS    | 选返回值版本更容易测试，理由也对：测试代码能直接比较返回值。                                                                                                         |
| B2  | PASS    | 能拆出读取输入、生成计划、评分答案、打印报告，并给出函数名。                                                                                                         |
| B3  | PASS    | 能说明 `set(tasks)` 是任务名去重，不是真正的标签去重；更合适来源是每个任务自带标签列表。                                                                                    |
| B4  | PASS    | `study_hours` 比 `time` 更精确，能说明小时、分钟、次数、截止时间等歧义。                                                                                        |
| B5  | PASS    | 工具注册表选 `dict` 正确，因为需要从工具名映射到真实函数对象。                                                                                                    |
| C1  | RETRY   | 函数思路接近，但代码块含 `U+00A0`，实际会报 `SyntaxError`。另外 `main()` 里调用后没有接住返回值；本题重点是写一个干净的 `make_plan()`，返回精确 list，不需要主程序。                           |
| C2  | PASS    | `return plan` 放到 `for` 循环外，能返回 3 条计划。                                                                                                  |
| C3  | RETRY   | 判断逻辑大体对，但代码块实际不可运行：开头多余缩进和 `U+00A0` 会导致 Python 报错；示例调用里还把“调用者”写成了“给者”。                                                                 |
| C4  | RETRY   | 需要保存旧任务名和旧状态，再迁移到新 key。`task_status[task4]` 会把 `task4` 当变量，且直接写 `"todo"` 会丢掉旧状态。                                                       |
| C5  | PARTIAL | 主流程方向对：读输入、转整数、调用 `make_plan()`、遍历打印。但代码块仍含 `U+00A0`，实际运行会失败；如果实现 `make_plan()`，返回字符串也建议继续沿用中文题目格式。                                    |
| C6  | PASS    | PowerShell 命令正确，绝对路径也用引号包起来了。                                                                                                          |
| D1  | PASS    | 能区分 `add_task()` / `finish_task()` 会修改传入数据，`unique_tags()` 返回新结果，这个理解不错。                                                               |
| D2  | PASS    | `rename_task()` 核心逻辑正确，能同步迁移 `task_status`。                                                                                            |
| D3  | PASS    | `enumerate(topics, start=1)` 用得对，返回字符串也符合题目要求。                                                                                         |
| D4  | RETRY   | 测试思路方向对，但断言字符串错了：题目要求第一条是 `"第1天：学习 练习函数"`，你的断言少了 `学习`。另外中文说明不能直接混在 Python 代码里运行。                                                       |
| E1  | PARTIAL | 你写“暂时没有”，但 A3、C4、D4 暴露出 list/dict/set 访问和测试精确性还不稳。                                                                                     |
| E2  | PARTIAL | 你可能不是猜的，但需要把“不确定”识别出来，这也是学习能力的一部分。                                                                                                     |
| E3  | RETRY   | 现在还不能说“没有需要复习的”。明天最该复习的是 list/dict/set 访问方式、任务状态迁移、可运行代码格式。                                                                            |
| E4  | PARTIAL | 你能说出一部分“输入、处理、输出”，但 A1 的例子把参数顺序写反，说明还需要再精确一次。                                                                                          |
| E5  | PARTIAL | 可以准备进入 P0-05，但在正式写文件前先完成下面第二轮订正，尤其是可运行代码和精确字符串。                                                                                        |

## 必须订正

只重做下面 7 个点即可，不需要重写整套题：

1. A1：重新解释 `goal`、`days`、`plan`，并写出正确调用示例。
2. A3：重新判断 list / dict / set 的访问方式。
3. C1：用普通空格重写最小 `make_plan(goal, days)`。
4. C3：用普通空格重写 `score_answer(answer)`，保证代码能运行。
5. C4：写出同步迁移 `task_status` 的有效 Python 代码。
6. C5：重写一个可运行的 `main()` 流程。
7. D4：修正手动测试，第一条字符串要和题目完全一致，说明文字不要混进 Python 代码。

## 第二轮订正作答区

直接在每道题下面的 `python` 代码块里回答。注意：代码缩进只用普通空格，不要从富文本里复制特殊空格。

1. A1：下面函数里，`goal`、`days`、`plan` 分别代表什么？请用“输入 / 处理 / 输出”回答，并写出正确调用示例。

   ```python
   def make_plan(goal, days):
       plan = []
       for i in range(days):
           plan.append(f"第{i + 1}天：学习 {goal}")
       return plan
   ```

   ```python
`days` 是循环次数、`plan` 是返回给调用者的 list，这些方向对。但 `goal` 应是学习目标字符串，不是“循环的字符串”；调用示例应是 `make_plan("Python", 3)`，不是 `make_plan(3, "Python")`。
   ```

2. A3：逐行判断下面代码哪些正确、哪些错误，并说明原因。

   ```python
   tasks = ["read", "code", "review"]
   task_status = {"read": "done", "code": "todo", "review": "todo"}
   tags = {"Python", "function", "Agent"}

   print(tasks[0])
   print(tasks["read"])
   print(task_status["read"])
   print(tags["Python"])
   print("Python" in tags)
   ```

   ```python
`tasks[0]`、`task_status["read"]`、`"Python" in tags` 正确；`tasks["read"]` 错，因为 list 下标应是整数；`tags["Python"]` 错，因为 set 不能像 dict 一样按 key 取值。
   ```

3. C1：重写 `make_plan(goal, days)`。调用 `make_plan("练习函数", 3)` 时必须返回下面这个 list，字符串要完全一致，函数里不要直接 `print()`。

   ```python
   [
       "第1天：学习 练习函数",
       "第2天：学习 练习函数",
       "第3天：学习 练习函数",
   ]
   ```

   ```python
def make_plan(goal, days):
	plan=[]
    for i in range(days):
      plan.append(f"第{i+1}天：学习 {goal}")
    return plan
   ```

4. C3：重写 `score_answer(answer)`，让下面三个调用分别返回注释里的结果。注意代码要能直接运行。

   ```python
   score_answer("太短")                          # "retry"
   score_answer("因为 return 会把结果交给调用者")   # "good"
   score_answer("return 会返回函数结果")           # "pass"
   ```

   ```python
def main():
    print(score_answer("太短"))                         # 返回 "retry"
    print(score_answer("因为 return 会把结果交给者"))  # 返回 "good"
    print(score_answer("return 会返回函数结果"))          # 返回 "pass"

def score_answer(answer):
    if len(answer) < 10:
        return "retry"
    elif "结果" in answer and "调用者" in answer :
        return "good"
    else:
        return "pass"
if __name__ == "__main__":
    main()
   ```

5. C4：把 `tasks[0]` 从 `"task1"` 改成 `"task4"`。写出有效 Python 代码，同步迁移 `task_status`：先保存旧任务名和旧状态，再写入新 key，最后删除旧 key。

   ```python
   tasks = ["task1", "task2"]
   task_status = {"task1": "todo", "task2": "done"}
   ```

   ```python
tasks = ["task1", "task2"]
task_status = {"task1": "todo", "task2": "done"}

old_name = tasks[0]
new_name = "task4"

tasks[0] = new_name
old_status = task_status[old_name]
task_status[new_name] = old_status
del task_status[old_name]

print(tasks)
print(task_status)
   ```

6. C5：写一个可运行的小主程序流程：读取 `goal` 和 `days`，调用 `make_plan(goal, days)` 得到 `plan`，最后用 `for` 循环逐行打印计划。可以假设 `make_plan()` 已经写好。

   ```python
def main():
  goal=input("请输入目标: ")
  days=int(input("请输入天数: "))
  plans= make_plan(goal, days)
  for plan in plans:
     print(plan)

def make_plan(goal, days):
    plan = []
    for i in range(days):
      plan.append(f"Day {i+1}: {goal}")
    return plan
    
if __name__ == "__main__":

  main()
   ```

7. D4：修正手动测试思路。至少检查返回类型、列表长度、第一条字符串、是否没有直接打印。第一条字符串必须和题目要求完全一致；中文说明可以写成注释。

   ```python
from io import StringIO
from contextlib import redirect_stdout

buf = StringIO()

with redirect_stdout(buf):
    result = make_plan("练习函数", 3)

assert isinstance(result, list), "返回值应该是 list"
assert len(result) == 3, "列表长度应该是 3"
assert isinstance(result[0], str), "第一条应该是字符串"

# 按你的函数约定检查第一条内容
assert result[0] == "第1天：练习函数", "第一条内容不符合预期"

assert buf.getvalue() == "", "函数不应该直接 print"

print("手动检查通过")
#思路是：先把 make_plan("练习函数", 3) 的返回值保存起来，再逐项检查它是不是列表、长度是不是 
#3、第一项是不是预期字符串，同时用 redirect_stdout 捕获输出，确认函数没有直接打印内容。
   ```

## 第二轮批改

结论：RETRY

这轮已经改对了 3 个关键点：A1 的参数角色、A3 的 `list` / `dict` / `set` 访问方式、C4 的任务状态迁移。尤其是 C4，这次能正确保存旧任务名、迁移旧状态、删除旧 key，说明“列表任务名”和“状态字典 key”要同步这件事已经抓住了。

还不能 PASS 的原因只剩代码精度：

- C1、C3、C5 仍然含有不间断空格 `U+00A0`，Python 编译会直接失败。
- C3 的示例调用里仍然把“调用者”写成了“给者”。
- C5 的 `make_plan()` 返回格式仍是英文 `Day {i+1}: {goal}`，和本套题的中文计划格式不一致。
- D4 的断言仍然少了 `学习`，应该检查 `"第1天：学习 练习函数"`。

统计：PASS 3，RETRY 4。

| 题号  | 结果    | 反馈                                                                                       |
| --- | ----- | ---------------------------------------------------------------------------------------- |
| A1  | PASS  | 能说出 `goal` 是学习目标字符串、`days` 是循环次数、`plan` 是返回给调用者的 list，调用示例也改成了 `make_plan("Python", 3)`。 |
| A3  | PASS  | 这次能明确区分：list 用整数下标，dict 用 key，set 用 `in` 做成员判断，不能像 dict 一样取 key。                         |
| C1  | RETRY | 思路接近，但代码里仍有 `U+00A0`，实际会报 `SyntaxError`。下一轮只写函数，不要写 `main()`。                            |
| C3  | RETRY | 逻辑方向对，但代码仍有 `U+00A0`，实际会报 `SyntaxError`；示例调用里的“给者”也要改回“调用者”。                             |
| C4  | PASS  | 迁移逻辑正确：保存旧任务名，更新 `tasks`，读取旧状态，写入新 key，删除旧 key。                                          |
| C5  | RETRY | 主流程结构对，但代码仍有 `U+00A0`；`make_plan()` 如果一起写，建议继续使用 `"第{i + 1}天：学习 {goal}"` 的中文格式。          |
| D4  | RETRY | 能编译，但关键断言仍然错了。题目要求第一条是 `"第1天：学习 练习函数"`，不是 `"第1天：练习函数"`。                                  |

## 第三轮最小订正作答区

只补下面 4 个点。请手打缩进，统一用普通空格，不要从富文本复制缩进。

1. C1：只写 `make_plan(goal, days)` 函数。调用 `make_plan("练习函数", 3)` 时必须返回下面这个 list，字符串要完全一致，函数里不要 `print()`。

   ```python
   [
       "第1天：学习 练习函数",
       "第2天：学习 练习函数",
       "第3天：学习 练习函数",
   ]
   ```

   ```python
def make_plan(goal,days):
		plan=[]
		for i in range(days):
			plan.append(f"第{i+1}天：学习 {goal}")
		return plan
   ```

2. C3：只写 `score_answer(answer)` 函数。不要写 `main()`，不要写测试打印。让下面三个调用分别返回注释里的结果。

   ```python
   score_answer("太短")                          # "retry"
   score_answer("因为 return 会把结果交给调用者")   # "good"
   score_answer("return 会返回函数结果")           # "pass"
   ```

   ```python
def score_answer(answer):
	if len(answer) < 10:
		return "retry"
	elif "结果" in answer and "调用者" in answer :
		return "good"
	else:
		return "pass"
   ```

3. C5：写一个可运行的小主程序流程。可以假设 `make_plan()` 已经存在，只写 `main()` 和入口判断即可。

   ```python
def main():
	goal=input("请输入目标: ")
	days=int(input("请输入天数: "))
	plans= make_plan(goal, days)
	for plan in plans:
		print(plan)
if __name__ == "__main__":
	main()
   ```

4. D4：修正手动测试。第一条字符串必须检查 `"第1天：学习 练习函数"`；中文说明写成注释。

   ```python
from io import StringIO
from contextlib import redirect_stdout
buf = StringIO()
with redirect_stdout(buf):
    result = make_plan("练习函数", 3)
assert isinstance(result, list)
assert len(result) == 3
assert isinstance(result[0], str)
# 按你的函数约定检查第一条内容
assert result[0] == "第1天：学习 练习函数", "第一条内容不符合预期"
assert buf.getvalue() == ""
print("手动检查通过")
   ```

## 第三轮批改

结论：PASS

这轮四个剩余问题都改到位了：

- C1 的 `make_plan(goal, days)` 能返回题目要求的 3 条中文计划，字符串完全一致。
- C3 的 `score_answer(answer)` 能让三个样例分别返回 `"retry"`、`"good"`、`"pass"`。
- C5 的主程序流程清楚：读取输入、转换 `days`、调用 `make_plan()`、遍历打印。
- D4 的手动测试已经检查返回类型、列表长度、第一条字符串和函数是否直接打印。

统计：PASS 4，RETRY 0。

| 题号 | 结果 | 反馈 |
| --- | --- | --- |
| C1 | PASS | 功能正确，返回值与题目要求一致。代码里已经没有 `U+00A0`。后续建议统一使用 4 个普通空格缩进，少用 tab。 |
| C3 | PASS | 条件判断有效，三个样例都能得到目标结果。 |
| C5 | PASS | 主程序流程可运行；本题允许假设 `make_plan()` 已存在，所以不需要重复写函数实现。 |
| D4 | PASS | 手动测试思路正确，`redirect_stdout` 能检查函数没有直接打印，第一条字符串也修正为 `"第1天：学习 练习函数"`。 |

## 稳定概念同步判断

本轮已 PASS，稳定概念已同步到：

- [[../../02-Concepts/Python/函数与参数(Functions)|Python 函数、参数与 return]]：补充可测试函数检查清单、`score_answer()` 例子、`redirect_stdout` 手动测试和缩进注意事项。
- [[../../02-Concepts/Python/list·dict·set 容器|Python list、dict、set]]：补充 `list` / `dict` / `set` 的访问方式对比，以及任务改名时同步迁移 `task_status` 的模式。
